# partis-zig-core

Zig reimplementation of partis's C++ computational core: [ham](https://github.com/psathyrella/ham) (HMM compiler) and ig-sw (Smith-Waterman aligner). Exposes a C ABI for direct integration with the partis Python driver.
